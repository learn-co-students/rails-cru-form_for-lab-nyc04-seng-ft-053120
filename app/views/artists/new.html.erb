<%= form_for @artist, url: {action: "create"} do |f| %>
  <label>Artist Name: </label>
  <%= f.text_field :name %><br>
  <label>Bio: </label>
  <%= f.text_field :bio %><br>

  <%= f.submit %>
<% end %>